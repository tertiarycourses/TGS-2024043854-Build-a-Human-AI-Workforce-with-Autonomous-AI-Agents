# WSQ assessment materials (confidential — never pushed)
/assessment/
/assessment\ /

# Secrets / env
.env
.env.*
*.pem
*.key
*.p12
*.pfx
credentials.json
secrets.json
id_rsa
id_ed25519

# Paperclip local install (learners clone this themselves — see LEARNER_GUIDE.md)
/paperclip/
/data/
paperclip-workspace/

# Python / tooling
__pycache__/
*.pyc
.venv/

# OS / editor
.DS_Store
.playwright-mcp/

# Superseded courseware binaries + transient locks (kept locally, not in git)
courseware/archive/
.claude/scheduled_tasks.lock

# Assessment reference material (confidential — Drive only)
/reference/
