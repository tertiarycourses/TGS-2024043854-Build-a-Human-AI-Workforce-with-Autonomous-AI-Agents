"""
Domain 2 — OpenClaw. Labs 15-24.

Running use case: *Nimbus Supplies* — automating a small business back-office.
The arc runs install -> providers -> channels -> skills -> tools -> commands ->
crons -> memory -> security -> multi-agent Sales/Research/Ops team.
"""

DOMAIN2 = [
    dict(
        num=15,
        topic=2,
        title="Install OpenClaw",
        objective="LO1: Install and configure an autonomous AI agent platform locally and on a container/VPS.",
        desc="The learner installs the OpenClaw platform on their own machine (or a cloud VPS) by first installing Node.js 24 LTS, then installing OpenClaw via npm and running the onboarding wizard, and verifies the install with doctor and gateway status. This gateway becomes the back-office brain of the Nimbus Supplies running example.",
        build="A running OpenClaw gateway daemon (local or VPS) verified with openclaw doctor",
        services="OpenClaw, Node.js 24 LTS, npm, openclaw gateway",
        steps=[
            ("Watch the OpenClaw reference playlist", "https://www.youtube.com/watch?v=q5YFlpVlmkI&list=PLHutrxqbP1BwAQf6dROCLZqK0PLpGu35J"),
            ("Install Node.js 24 LTS on macOS with Homebrew and verify", "brew install node@24"),
            ("Install OpenClaw globally via npm", "npm install -g openclaw@latest"),
            ("Run the interactive onboarding wizard that creates ~/.openclaw and the gateway", "openclaw onboard"),
            ("Verify the install version", "openclaw --version"),
            ("Run the health check (Node, gateway daemon, providers, channels)", "openclaw doctor"),
            ("Confirm the gateway is running, starting it if needed", "openclaw gateway start"),
        ],
        test="openclaw --version prints a version, openclaw doctor shows green checks for Node 24 and the gateway daemon (provider/channel checks pending until Labs 16-17), and openclaw gateway status reports running.",
    ),
    dict(
        num=16,
        topic=2,
        title="Models & Providers",
        objective="LO2: Connect an LLM model/provider to an agent via OAuth or API key.",
        desc="The learner connects a language model to OpenClaw, trying the supported provider options — OAuth sign-ins (OpenAI Codex, MiniMax) and API-key providers (Anthropic, DeepSeek, OpenRouter) — learns where credentials are stored under ~/.openclaw/auth/, hot-swaps between models, and runs a Nimbus Supplies smoke test.",
        build="A Nimbus Supplies agent wired to a working model provider that passes openclaw model test",
        services="OpenClaw, OpenAI Codex / MiniMax (OAuth), Anthropic / DeepSeek / OpenRouter (API key)",
        steps=[
            ("Watch the OpenClaw reference playlist", "https://www.youtube.com/watch?v=q5YFlpVlmkI&list=PLHutrxqbP1BwAQf6dROCLZqK0PLpGu35J"),
            ("See the available models and the current selection", "openclaw models list", "Which models can you use, and which one is active right now?"),
            ("Option A - sign in to OpenAI Codex via OAuth", "openclaw models auth login --provider openai-codex"),
            ("Option C - set an Anthropic API key and select a Claude model", "openclaw config set model anthropic/claude-opus-4-7", "Switch your model to Anthropic's Claude Opus 4.7"),
            ("Test that the active model responds", "openclaw model test", "Say hello so I can confirm your model connection works"),
            ("Persist an API key so it survives reboots", "echo 'export ANTHROPIC_API_KEY=\"sk-ant-...\"' >> ~/.zshrc"),
            ("Hot-swap the global default model and confirm", "openclaw model use deepseek/deepseek-v4", "Make DeepSeek v4 your default model from now on"),
            ("Give the agent a Nimbus Supplies smoke test from CLI chat", "openclaw", "Introduce yourself in two sentences as the Nimbus Supplies back-office assistant"),
        ],
        test="openclaw models list shows the connected models, openclaw model current prints the active one, openclaw model test returns a short successful completion with no auth error, and OAuth credentials appear under ~/.openclaw/auth/.",
    ),
    dict(
        num=17,
        topic=2,
        title="Channels",
        objective="LO2: Connect multiple messaging channels to one agent gateway.",
        desc="The learner gives customers a way to reach the Nimbus Supplies agent by creating a Telegram bot end-to-end via BotFather, registering it with a token, pairing a WhatsApp number by scanning a QR code, and running both channels at once through a single OpenClaw gateway so the same agent answers on either platform.",
        build="Telegram and WhatsApp both live on one OpenClaw gateway, answered by the same Nimbus Supplies agent",
        services="OpenClaw, Telegram (BotFather), WhatsApp, openclaw channel",
        steps=[
            ("Watch the OpenClaw reference playlist", "https://www.youtube.com/watch?v=q5YFlpVlmkI&list=PLHutrxqbP1BwAQf6dROCLZqK0PLpGu35J"),
            ("Create a Telegram bot in BotFather with /newbot and copy the HTTP API token", ""),
            ("Register the Telegram channel with the bot token", "openclaw channel add telegram --token 123456789:ABC-DEF1234ghIkl-zyx57W2v1u123ew11", "Connect Telegram using the bot token I got from BotFather"),
            ("Start the Telegram channel", "openclaw channel start telegram", "Start the Telegram channel and confirm it is live"),
            ("Add the WhatsApp channel to print a pairing QR code", "openclaw channel add whatsapp", "Add WhatsApp as a channel and show me the pairing QR code"),
            ("Start WhatsApp, then scan the QR from WhatsApp -> Linked Devices -> Link a Device", "openclaw channel start whatsapp", "Start the WhatsApp channel so I can scan the QR code"),
            ("Confirm both channels are running simultaneously", "openclaw channel list", "Which channels are you listening on right now?"),
        ],
        test="openclaw channel list shows telegram and whatsapp both running, a message to the Telegram bot gets an agent reply, a message to the linked WhatsApp account gets an agent reply, and openclaw gateway status shows both connected.",
    ),
    dict(
        num=18,
        topic=2,
        title="Skills",
        objective="LO3: Extend an agent with registry and custom skills.",
        desc="The learner installs and removes skills from the skills.sh registry, invokes them from chat, and builds a custom Nimbus Supplies skill (nimbus-quote) under ~/.openclaw/skills/ so the agent handles a recurring back-office task — drafting an itemised customer quote with GST — the same way every time.",
        build="Registry skills plus a custom nimbus-quote skill that produces itemised, GST-inclusive quotes",
        services="OpenClaw, skills.sh registry, ~/.openclaw/skills/, openclaw skills",
        steps=[
            ("Watch the OpenClaw reference playlist", "https://www.youtube.com/watch?v=q5YFlpVlmkI&list=PLHutrxqbP1BwAQf6dROCLZqK0PLpGu35J"),
            ("List the skills currently installed", "openclaw skills list", "What skills do you have installed?"),
            ("Install a useful skill from the skills.sh registry", "openclaw skills add web-research --source skills.sh", "Install the web-research skill from the skills.sh registry"),
            ("Invoke a skill from a channel with the /skill slash command", "/skill web-research \"Who are the main office-supplies wholesalers in Singapore?\""),
            ("Create the folder for a custom Nimbus Supplies skill", "mkdir -p ~/.openclaw/skills/nimbus-quote", "Create a new custom skill called nimbus-quote for drafting itemised customer quotes"),
            ("Author skill.md with front-matter (name, description) and the quoting instructions", ""),
            ("Restart the gateway so the new skill is picked up", "openclaw gateway restart", "Reload your skills so the new nimbus-quote skill is picked up"),
            ("Test the custom skill from a channel", "/skill nimbus-quote \"Customer: Acme Cafe. 10 reams recycled A4 paper, 5 boxes black pens.\""),
            ("The natural-language way — just message the agent: 'Install the github skill from ClawHub and show me what it can do'", ""),
        ],
        test="openclaw skills list shows the registry skills plus the custom nimbus-quote, /skill web-research runs end-to-end, and /skill nimbus-quote produces an itemised quote with a 9% GST line and standard terms, marking any unknown price as TBC instead of guessing.",
    ),
    dict(
        num=19,
        topic=2,
        title="Tools & Integrations",
        objective="LO3: Extend an agent with third-party tool integrations.",
        desc="The learner extends the Nimbus Supplies agent with two real integrations — Firecrawl (JS-rendered web scraping) and AgentMail (its own inbox/outbox) — then runs an end-to-end back-office task: research a supplier's website prices, draft an itemised quote with the Lab 18 skill, and email it to a customer. Tool profiles and allow/deny lists scope tools per channel.",
        build="A Firecrawl + AgentMail integrated agent that scrapes supplier prices and emails an itemised quote",
        services="OpenClaw, Firecrawl, AgentMail, openclaw tools",
        steps=[
            ("Watch the OpenClaw reference playlist", "https://www.youtube.com/watch?v=q5YFlpVlmkI&list=PLHutrxqbP1BwAQf6dROCLZqK0PLpGu35J"),
            ("Inspect the built-in tool groups (fs, web, runtime, media)", "openclaw tools list", "What tools do you have, grouped by category?"),
            ("Enable Firecrawl with its API key", "openclaw tools enable firecrawl --api-key \"$FIRECRAWL_API_KEY\"", "Enable the Firecrawl scraping tool with my API key"),
            ("Enable AgentMail with its API key and inbox address", "openclaw tools enable agentmail --api-key \"$AGENTMAIL_API_KEY\" --inbox bot@yourname.agentmail.to", "Set up AgentMail with my API key so you have your own inbox"),
            ("Run the end-to-end task from a channel: scrape prices, draft the quote, email it", ""),
            ("Scope tools on a public channel by denying shell exec", "openclaw channel set telegram --deny exec", "On the Telegram channel, block shell command execution"),
            ("Allow only the web tool group on that channel", "openclaw channel set telegram --allow group:web", "On Telegram, allow only the web tool group"),
            ("The natural-language way — just message the agent: 'Use the browser to pull today's top three AI headlines and save them to headlines.md'", ""),
        ],
        test="openclaw tools list --enabled shows firecrawl and agentmail, the agent scrapes a supplier URL and returns structured prices, an AgentMail send arrives in a real inbox, the end-to-end task produces an itemised quote email, and a channel set to --deny exec refuses shell-exec requests.",
    ),
    dict(
        num=20,
        topic=2,
        title="Commands",
        objective="LO4: Operate an agent through its CLI and in-chat command surface.",
        desc="The learner becomes fluent in the two ways to drive OpenClaw daily: the CLI commands (config, model, doctor, gateway, channel, tools, skills, cron) and the in-chat slash commands operators and customers use inside a channel, building a personal quick-reference table for running the Nimbus Supplies back office and streaming live gateway logs.",
        build="A personal quick-reference of the CLI and slash commands used to run Nimbus Supplies day to day",
        services="OpenClaw, openclaw CLI, in-chat slash commands",
        steps=[
            ("Watch the OpenClaw reference playlist", "https://www.youtube.com/watch?v=q5YFlpVlmkI&list=PLHutrxqbP1BwAQf6dROCLZqK0PLpGu35J"),
            ("Read and change settings with config", "openclaw config list", "Show me your current configuration settings"),
            ("Run the health check with auto-fix", "openclaw doctor --fix"),
            ("Control the gateway daemon that runs everything", "openclaw gateway restart"),
            ("Get your channel user ID for the Lab 23 allowlist", "/whoami"),
            ("Switch the model for one conversation only", "/model deepseek/deepseek-v4"),
            ("Watch activity live while you chat", "openclaw gateway logs --follow"),
        ],
        test="You can switch the model both from chat (/model, per-conversation) and globally from the CLI (openclaw model use), /whoami returns your platform user ID, openclaw gateway logs --follow streams live activity, and /help lists the available slash commands.",
    ),
    dict(
        num=21,
        topic=2,
        title="Cron Jobs & Heartbeat",
        objective="LO4: Automate agent work with scheduled crons and a monitoring heartbeat.",
        desc="The learner makes Nimbus Supplies run unattended by scheduling recurring tasks with cron (a nightly sales report and a weekly supplier price-check using Firecrawl) and monitoring uptime with the heartbeat, so the gateway self-checks, auto-restarts on failure, and can page an external monitor if it dies.",
        build="A nightly sales-report cron, a weekly price-check cron, and an enabled self-healing heartbeat",
        services="OpenClaw, openclaw cron, gateway heartbeat, Firecrawl + AgentMail",
        steps=[
            ("Watch the OpenClaw reference playlist", "https://www.youtube.com/watch?v=q5YFlpVlmkI&list=PLHutrxqbP1BwAQf6dROCLZqK0PLpGu35J"),
            ("Create the nightly sales-report cron posting to Telegram at 21:00", "openclaw cron create --schedule \"0 21 * * *\" --prompt \"Summarise today's Nimbus Supplies customer chats and any quotes sent.\" --channel telegram --name nightly-sales-report", "Every night at 9pm, summarise today's customer chats and quotes and post it to Telegram"),
            ("List and inspect your crons", "openclaw cron list", "What cron jobs do you have scheduled?"),
            ("Fire a cron on demand to test it now, ignoring the schedule", "openclaw cron run nightly-sales-report", "Run the nightly sales report right now so I can check it works"),
            ("Enable the heartbeat self-check at a 60s interval", "openclaw gateway heartbeat enable --interval 60s", "Turn on your heartbeat self-check every 60 seconds"),
            ("Check the heartbeat status", "openclaw gateway heartbeat status", "Is your heartbeat healthy? Give me the status"),
            ("Confirm auto-restart by stopping the gateway and waiting ~60s", "openclaw gateway stop", "Shut down your gateway - I want to check the heartbeat restarts you"),
            ("The natural-language way — just message the agent: 'Add a heartbeat task: every 30 minutes, check my inbox and flag anything urgent'", ""),
        ],
        test="openclaw cron list shows nightly-sales-report and weekly-price-check, openclaw cron run delivers a summary to Telegram immediately, openclaw gateway heartbeat status reports healthy, and after openclaw gateway stop the service manager restarts the gateway within about a minute.",
    ),
    dict(
        num=22,
        topic=2,
        title="Memory & Context",
        objective="LO2: Give an agent persistent, user-scoped memory.",
        desc="The learner gives the Nimbus Supplies agent durable memory so it recalls customer preferences across conversations: using the in-chat /memory command, inspecting where OpenClaw persists state under ~/.openclaw/, teaching a customer's standing preference, clearing the conversation to prove it is real memory, and confirming recall in a later separate chat.",
        build="An agent that persistently remembers a customer's standing preferences and applies them in a fresh conversation",
        services="OpenClaw, /memory, /clear, ~/.openclaw/ state",
        steps=[
            ("Watch the OpenClaw reference playlist", "https://www.youtube.com/watch?v=q5YFlpVlmkI&list=PLHutrxqbP1BwAQf6dROCLZqK0PLpGu35J"),
            ("See what OpenClaw stores on disk", "ls -la ~/.openclaw/", "Show me what you store in your ~/.openclaw state folder"),
            ("Inspect your current per-user memory from chat", "/memory"),
            ("Teach the agent a durable customer preference in plain language", ""),
            ("Clear the conversation to prove it is real memory, not just context", "/clear"),
            ("Recall in the cleared chat by requesting a quote that needs the remembered facts", ""),
            ("Back up memory before major changes", "cp -R ~/.openclaw/ ~/openclaw-backup-$(date +%F)/openclaw-state", "Back up all your memory and state to a dated folder in my home directory"),
        ],
        test="/memory shows the customer preferences after you teach them, after /clear the /memory list still shows them (short-term context cleared, long-term memory retained), and a fresh quote request for the customer automatically applies recycled paper, Tuesday delivery and the correct recipient name without repetition.",
    ),
    dict(
        num=23,
        topic=2,
        title="Security",
        objective="LO4: Secure an agent with key management, allowlists, sandboxing and audit.",
        desc="The learner hardens the Nimbus Supplies deployment before real customers: storing API keys as environment variables (never in cleartext config), restricting who can DM the bot with allowlists, applying per-channel tool deny lists, tightening the code-execution sandbox (timeout/memory/network), reviewing the audit log, and rotating provider keys.",
        build="A hardened agent with keys in env, DM allowlists, per-channel deny lists, a capped sandbox and an exported audit log",
        services="OpenClaw, allowlists, tool deny lists, exec/python sandbox, gateway logs",
        steps=[
            ("Watch the OpenClaw reference playlist", "https://www.youtube.com/watch?v=q5YFlpVlmkI&list=PLHutrxqbP1BwAQf6dROCLZqK0PLpGu35J"),
            ("Restrict who can DM the Telegram bot to known user IDs", "openclaw channel set telegram --allow-users 12345678,87654321 --pair-mode allowlist", "Only allow Telegram users 12345678 and 87654321 to message you; block everyone else"),
            ("Apply a per-channel tool deny list (no shell or destructive file ops)", "openclaw channel set telegram --deny exec,fs.write,fs.delete", "On Telegram, deny shell exec and any file writes or deletes"),
            ("Tighten the code-execution sandbox (time, memory, no network)", "openclaw tools config exec --timeout 10s --memory 256mb --network deny", "Cap your exec sandbox at 10 seconds, 256MB memory and no network access"),
            ("Confirm keys are not sitting in cleartext in the config", "cat ~/.openclaw/config.toml | grep -i key"),
            ("Review the audit log, filtering for blocked exec attempts", "openclaw gateway logs --filter tool=exec"),
            ("Export the audit log for weekly review", "openclaw gateway logs --export ~/openclaw-audit-$(date +%F).log"),
        ],
        test="cat ~/.openclaw/config.toml does not contain raw API keys, a user not on the Telegram allowlist gets a not-authorized response, openclaw channel show telegram lists the deny list and messaging profile, and the audit log shows blocked exec attempts on a locked channel.",
    ),
    dict(
        num=24,
        topic=2,
        title="Multi-Agent Workforce",
        objective="LO5: Orchestrate cooperating multi-agent teams to deliver a business outcome.",
        desc="In the OpenClaw capstone the learner splits the single agent into three cooperating agents — Sales (customer-facing on Telegram/WhatsApp), Research (supplier scouting with Firecrawl), and Ops (email, quotes, nightly crons) — each with least-privilege tools, and runs one end-to-end business scenario across all three, with a verified fallback using isolated OPENCLAW_HOME instances that cooperate via AgentMail and a shared folder.",
        build="A three-agent Sales/Research/Ops workforce that runs a customer order end-to-end into a real quote email",
        services="OpenClaw, openclaw agent (or isolated OPENCLAW_HOME instances), Firecrawl, AgentMail",
        steps=[
            ("Watch the OpenClaw multi-agent setup reference video", "https://www.youtube.com/watch?v=q5YFlpVlmkI&list=PLHutrxqbP1BwAQf6dROCLZqK0PLpGu35J&index=1"),
            ("Create three named agents (or use isolated instances if unavailable)", "openclaw agent create sales", "Create a new agent named Sales to handle customer-facing chats"),
            ("Give the Sales agent least-privilege tools (web + quoting, no shell/fs)", "openclaw agent set sales --allow group:web --deny exec,fs.write,fs.delete", "Give the Sales agent web and quoting tools only, with no shell or file writes"),
            ("Restrict Research to scraping only", "openclaw agent set research --allow firecrawl,web_search,browser --deny exec,fs.write", "Restrict the Research agent to Firecrawl, web search and browser only"),
            ("Attach the customer channels to the Sales agent", "openclaw channel set telegram --agent sales", "Route the Telegram channel to the Sales agent"),
            ("Run a Research fallback instance with an isolated config dir", "OPENCLAW_HOME=~/.openclaw-research openclaw onboard"),
            ("Give Ops the nightly consolidation cron", "openclaw cron create --schedule \"0 21 * * *\" --prompt \"Compile today's Nimbus Supplies orders and quotes sent, and list anything waiting on Research.\" --channel telegram --name ops-nightly-report", "Every night at 9pm, compile today's orders and quotes and post them to Telegram"),
            ("The natural-language way — just message the agent: 'Spawn two sub-agents to research our top two competitors in parallel, then compare their pricing'", ""),
        ],
        test="Three agents (or isolated instances) each have a distinct least-privilege tool set (Sales cannot exec, Research cannot write files, Ops is not on a public channel), a customer Telegram message produces a real quote email combining memory, the nimbus-quote skill, a Firecrawl price lookup and AgentMail delivery, and the nightly cron posts a summary referencing the same order.",
    ),
    dict(
        num=25,
        topic=2,
        title="Dreaming — Idle Reflection & Self-Improvement",
        objective="LO3: Use OpenClaw 'dreaming' so the agent reflects during idle time.",
        desc="Enable OpenClaw's 'dreaming' feature so the Nimbus Supplies agent uses idle time to review its memory, consolidate what it has learned, and generate useful follow-up actions and skill improvements — then inspect what it produced. Reference the OpenClaw functions playlist for the walkthrough.",
        build="An agent that, when idle, 'dreams' — reflecting on memory and proposing follow-ups you can review.",
        services="OpenClaw, dreaming, memory",
        steps=[
            ("Watch the OpenClaw functions reference video", "https://www.youtube.com/watch?v=q5YFlpVlmkI&list=PLHutrxqbP1BwAQf6dROCLZqK0PLpGu35J"),
            ("Enable the dreaming feature in OpenClaw settings/config", "openclaw config set dreaming.enabled true", "Turn on dreaming so you reflect and consolidate memory when idle"),
            ("Let the agent sit idle so a dream cycle runs (or trigger one)", "openclaw dream run", "Run a dream cycle now instead of waiting for idle time"),
            ("Review what the dream produced (insights, follow-ups, skill tweaks)", "openclaw dream list", "Show me the insights and follow-ups from your last dream"),
            ("Accept a useful follow-up the agent proposed", ""),
        ],
        test="A dream cycle runs during idle time and produces reviewable insights/follow-ups from the agent's memory.",
    ),
    dict(
        num=26,
        topic=2,
        title="Use Cases & Key Functions",
        objective="LO3: Apply OpenClaw's key functions to real back-office use cases.",
        desc="Tie the OpenClaw track together with practical use cases for Nimbus Supplies — a Google Workspace CLI assistant, a data-analytics agent, and a social-media-marketing agent — combining channels, skills, tools, memory, dreaming and crons into end-to-end automations. Each use case has its own reference video.",
        build="One or more end-to-end OpenClaw automations (Workspace, analytics or social media) solving a real use case.",
        services="OpenClaw, skills, tools, Google Workspace, crons, memory",
        steps=[
            ("Use case A — Google Workspace CLI assistant (reference video)", "https://www.youtube.com/watch?v=_ssB1YXRRtk&list=PLHutrxqbP1BwAQf6dROCLZqK0PLpGu35J&index=9"),
            ("Use case B — Data Analytics agent (reference video)", "https://www.youtube.com/watch?v=RLUa-j-MpgQ&list=PLHutrxqbP1BwAQf6dROCLZqK0PLpGu35J&index=10"),
            ("Use case C — Social Media Marketing agent (reference video)", "https://www.youtube.com/watch?v=q5YFlpVlmkI&list=PLHutrxqbP1BwAQf6dROCLZqK0PLpGu35J&index=1"),
            ("Pick a use case and compose the channels, skills and tools needed for it", ""),
            ("Run the automation end-to-end and verify the outcome", ""),
            ("Schedule it or hand it to the multi-agent team from Lab 24", ""),
        ],
        test="A real use case (Google Workspace, data analytics or social media) runs end-to-end using OpenClaw's channels, skills, tools and crons.",
    ),
]
